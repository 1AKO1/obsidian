**java基础：**

1. java和python区别。
2. java方法访问修饰符：default : 在同一包内可见，不使用任何修饰符。protected : 对同一包内的类和所有子类可见 。  
3. java集合：list用过吗，怎么扩容，删除怎么办。
4. ArrayList的有一个field叫做modCount是做什么用的？**迭代器的快速失败行为（fail-fast）**: `modCount` 被 `ArrayList` 的迭代器用来实现快速失败行为。在迭代器创建之后，如果集合被结构性地修改了，迭代器会检测到 `modCount` 的变化，从而抛出 `ConcurrentModificationException` 异常来避免不确定的行。
5. CopyOnWriteArrayList。
6. hashmap: put过程。发2个一样的对象会怎样？如何确定是同一个元素？检测是否是同一个键：先比较hash还是比较equals。1.8：一个桶存储的链表长度大于 8且hashmap容量大于64时，链表才能转成红黑树。
7. 为啥不一直用红黑树？红黑树节点的大小大约是常规节点的两倍，因此我们仅在容器包含足够的节点以保证使用时才使用它们  。
8. ConcurrentHashMap：put过程。put的时候正在扩容，扩容可以多线程。
9. 为什么用红黑树？红黑树和AVL树。红黑树插入效率高、查找效率低。
10. 有序的map有哪些？LinkedHashMap双链表：put过程和hashmap区别（父子类，单独加了一个函数实现，模板模式）。TreeMap红黑树
11. Java中两个double类型的数怎么判断相等？double转换成long型数据、误差法、字符串。0.1不能表示，运算后有误差
12. 计算机如何存储double？符号位、指数、位数。



**多线程：**

1. CompletableFuture用过吗？
2. 多线程死锁？
3. volatile关键字：只保证多线程操作的可见性，不保证原子性。
4. 线程池：为啥要有线程池？线程池核心参数？
5. ThreadLocal：每个线程Thread都维护了一个ThreadLocal.ThreadLocalMap类型的对象。ThreadLocal变量为key，以我们指定的值为value。hash冲突怎么解决？线性探测   
6. synchronized和ReentrantLock有什么区别呢？释放锁               
7. CountDownLath怎么用？
8. 2个线程交替打印0101 



**设计模式：**

说几个。单例模式，双重校验锁写一个。

```java
1.	public class Singleton {    
2.	    private volatile static Singleton singleton;    
3.	    private Singleton (){}    
4.	    public static Singleton getSingleton() {    
5.	    if (singleton == null) {    
6.	        synchronized (Singleton.class) {    
7.	        if (singleton == null) {    
8.	            singleton = new Singleton();    
9.	        }    
10.	        }    
11.	    }    
12.	    return singleton;    
13.	    }    
14.	} 
```



**mysql**

1. 索引的数据结构：B树，B+，红黑。
2. 数据库聚簇索引和非聚簇索引的区别？ 叶子节点直接是数据行本身、每个表只能有一个聚簇索引。非聚簇索引的叶子节点存储的是索引键值及与数据行相关的指针或行标识符。
3. 事务隔离级别                    
4. 已提交读和可重复读依赖MVCC。在每一行加入隐藏的字段。它实现了不加锁的读操作。
5. mysql读会加锁吗？
6. 什么是快照读？什么当前读？
7. 查询一般是快照读、写操作当前读。

![image-20240910200337430](/Users/zyl/Library/Application Support/typora-user-images/image-20240910200337430.png)

1. 快照何时生成？事务启动时，这个快照就是当前事务在整个生命周期中数据的一致性视图。
   2. 事务 ID 的，系统会维护一个单调递增的全局事务 ID 为每个新事务分配。隐藏列 DB_TRX_ID：每个数据行都有一个隐藏的列 DB_TRX_ID，记录最后一次修改该行的事务 ID。读取数据时，InnoDB 会根据 `DB_TRX_ID` 和撤销日志来判断当前事务是否能看到该行的版本。
3. mysql，RR怎么解决的幻读。MVCC+Next-Key Locks.  
   4. MVCC读事务开始的快照，已有的行，看快照版本、不会看到别的事务未提交的数据。但是，MVCC不能防止其他事务插入新的行。因此，如果当前事务再次执行范围查询，可能会看到之前不存在的新行，这就是幻读。
   5. 快照读没问题；当前读，比如写操作，不会读快照，会读最新的。此时需要锁。
6. 联合索引：weight,  age，height。KEY(weight,  age，height)

select * from table where weight > 100 and  age = 20 and  height > 20 

用到什么索引   

select * from table where weight = 100 and  age > 20 and  height > 20

数据库分页查询时如何避免性能问题？偏移量较大，性能会显著下降，因为数据库需要扫描和跳过大量的行。用索引、提前获取最后一条记录的标识加一个大于、覆盖索引避免回表。分表。





**JVM**

1. JVM 运行时数据区。     
2. jvm如何制造一个堆溢出、栈溢出、元空间溢出：元空间溢出：动态生成或加载大量类。
3. 垃圾回收算法     
4. 分代收集算法        
5. 对象怎么从新生代到老年代？  
6. G1 ：G1 的主要设计目标是提供可预测的低停顿时间。
7. Zgc ：低延迟垃圾收集器，旨在将垃圾收集器的暂停时间控制在 10ms 以内，即使在非常大的堆内存上也是如此（当前支持的堆大小为数 TB 级别）。ZGC 主要针对那些需要低延迟和大堆内存的应用程序。通过将对象状态信息嵌入指针的高位，实现了更加高效的并发标记和内存重定位。
   8. 传统的垃圾收集器在进行并发标记和重定位时通常需要读屏障（read barriers），来读取和更新对象状态。然而，着色指针将状态信息直接存储在指针中，从而减少了需要执行的读屏障操作，进一步提高了性能。




**计算机网络：**

1. 浏览器输入地址到展示的过程？DNS 解析、建立 TCP 连接、构建 HTTP 请求、网络层路由与交换
2. 为什么TCP链接需要三次握手，两次不可以么？       
3. TCP协议如何来保证传输的可靠性? 数据包校验、对失序数据包重排序、滑动窗口和应答机制、流量控制  





**操作系统：**

1. 进程和线程           
2. wc  -l              wc testfile                   
3. pwd              显示当前目录              
4. 内核态、用户态。



**redis**：

1. Redis为什么这么快   
2. Redis有哪几种持久化方式？优缺点是什么？   RDB的缺点是：宕机会丢失最后一次RDB后的数据，适合备份。  AOF的文件体积通常要大于RDB文件的体积。
3. Redis有哪些数据类型？   
4. hash怎么扩容？       
5. zset怎么实现？有序集合。      
6. 全局锁。         
7. 主从复制。
8. 集群模式和哨兵模式的区别？集群：大规模数据，负载均衡，水平扩展	哨兵：高可用性，小规模数据，不需要分片的场景



**Springboot**

1. 如何理解IoC和DI
2. spring如何解决循环依赖？        
3. bean的生命周期？有哪些步骤？延迟加载 getBean发生什么？ 
4. 注入到静态中      
5. Resource autowired。注入数组。     
6. 如何实现AOP  
7. 动态代理怎么选择。
8. 反射用的场景。



**MyBatis**

为什么要用mubatis？     

#{}和${}的区别？怎么获取一个sqlsession？ 





3.在Kafka中，ZooKeeper的作用是什么？

目前，Kafka使用ZooKeeper存放集群元数据、成员管理、Controller选举，以及其他一些管理类任务。之后，等KIP-500提案完成后，Kafka将完全不再依赖于ZooKeeper。

消息积压怎么办？

kafka什么时候会丢消息？   



**第一个项目：**

Spring Task实现定时任务来

埋点上报怎么做的，存储在哪里

JavaMail将信息发送到指定邮箱中进





**算法**

反转一个单链表。

示例:输入: 1->2->3->4->5->NULL

输出: 5->4->3->2->1->NULL



递归写法

~~~

class ListNode {
      int val;
      ListNode next;
      ListNode() {}
      ListNode(int val) { this.val = val; }
      ListNode(int val, ListNode next) { this.val = val; this.next = next; }
}

class Solution {
    public ListNode reverseList(ListNode head) {

    }
}
~~~



~~~java
public ListNode reverseList(ListNode head) {
    if (head == null || head.next == null) return head;
    ListNode p = reverseList(head.next);
		head.next.next = head; //head放到末尾
    head.next = null;        //  最后指向null
    return p;
}
~~~



**最长连续序列**

给定一个未排序的整数数组，找出最长连续序列的长度。

要求算法的时间复杂度为 O(n)。

示例:输入: [100, 4, 200, 1, 3, 2]输出: 4

解释: 最长连续序列是 [1, 2, 3, 4]。它的长度为 4。

~~~java
class Solution {
    public int longestConsecutive(int[] nums) {
        Set<Integer> num_set = new HashSet<Integer>();
        for (int num : nums) {
            num_set.add(num);	所有元素存到set里面
        }

        int longestStreak = 0;

        for (int num : num_set) {
            if (!num_set.contains(num-1)) { 只对前一个数没有的操作，找头然后向后算长度
                int currentNum = num;
                int currentStreak = 1;

                while (num_set.contains(currentNum+1)) {  算这个头开始的长度
                    currentNum += 1;
                    currentStreak += 1;
                }

                longestStreak = Math.max(longestStreak, currentStreak);
            }
        }

        return longestStreak;
    }
}

~~~



